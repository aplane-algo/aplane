# Attestor Signing Architecture

The canonical attested-signing architecture lives in
[ARCH_SPEC.md#attested-signing-and-attestor-nodes](ARCH_SPEC.md#attested-signing-and-attestor-nodes).

Compatibility-bearing HTTP, admin, storage, endpoint, policy, backup/restore,
and SDK contracts live in [ARCH_CONTRACTS.md](ARCH_CONTRACTS.md) and
[ARCH_HTTP_API.md](ARCH_HTTP_API.md).

Policy semantics live in [ARCH_POLICY.md](ARCH_POLICY.md), and the formal
attested-signing model lives in
[FORMAL_ATTESTED_SIGNING_MODEL.md](FORMAL_ATTESTED_SIGNING_MODEL.md).

Do not add new attestor contracts here. Update the unified architecture and
contract documents instead.
