\* TLC configuration for guarded_assembly.tla
\* See ../FORMAL_TLA_GUARDED_ASSEMBLY_MODEL.md for context and how to run.

SPECIFICATION Spec

CONSTANTS
    MaxEntries = 2

\* One-shot spec (Next == UNCHANGED vars): every initial state is one
\* assembly execution; the terminal state is not a deadlock.
CHECK_DEADLOCK FALSE

INVARIANT Safety
