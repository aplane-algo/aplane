\* TLC configuration for policy_precedence.tla
\* See ../FORMAL_TLA_POLICY_PRECEDENCE_MODEL.md for context and how to run.

SPECIFICATION Spec

\* No CONSTANTS are declared by the module — every domain is finite by
\* construction (booleans and small enums). The whole state space is
\* enumerable without bounds.

INVARIANT Safety
