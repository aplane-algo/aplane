\* Deep-bounds variant for make formal-test-deep (pre-release / scheduled
\* runs; not CI push). Metrics recorded in metrics_deep.json.
\* TLC configuration for plugin_signing.tla
\* See ../FORMAL_TLA_PLUGIN_SIGNING_MODEL.md for context and how to run.

SPECIFICATION Spec

CONSTANTS
    MaxSlots = 4

\* One-shot spec (Next == UNCHANGED vars): every initial state is one
\* submission attempt; the terminal state is not a deadlock.
CHECK_DEADLOCK FALSE

INVARIANT Safety
