# Bundled Plugins

This directory contains production plugins that APlane builds, packages, and
installs as bundled plugin catalog entries.

- `algokit-localnet`: supported LocalNet operations plugin for `apshell`

Source-only protocol examples live under `examples/external_plugins/`.
