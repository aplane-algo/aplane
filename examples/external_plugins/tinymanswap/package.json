{
  "name": "tinymanswap-generator",
  "version": "1.0.0",
  "description": "Transaction generator for Tinyman swaps",
  "main": "generate-swap.ts",
  "scripts": {
    "build": "tsc generate-swap.ts --outDir dist --module commonjs --target es2020 --esModuleInterop --resolveJsonModule --skipLibCheck",
    "generate": "ts-node generate-swap.ts"
  },
  "dependencies": {
    "@tinymanorg/tinyman-js-sdk": "5.1.3",
    "algosdk": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
